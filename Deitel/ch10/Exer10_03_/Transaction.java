/*
 * Escreva um código Java para começar a implementar o projeto da classe
 * Transaction especificada nas figuras 10.21 e 10.22. Certifique-se de
 * incluir atributos de tipo por referência private como base nas associações da
 * classe Transaction. Também se certifique de incluir os métodos get public que
 * fornecem acesso a qualquer desses atributos private que as subclasses exigem
 * para realizar suas tarefas.
 */

package ch10.Exer10_03_;

// Clsse abstrata Transaction representa uma transação do ATM
public abstract class Transaction {
/*    // atributos
    private int accountNumber; // indica conta envolvida
    private Screen screen; // Tela do ATM

    // construtor sem argumentos invocado pelas subclasses utilizando super()
    public Transaction()
    {
    } // Fim do construtor transaction sem argumentos

    // retorna o número da conta
    public int getAccountNumber()
    {
    } // fim do método getAccountNumber

    // retorna a referência à tela
    public Screen getScreen()
    {
    } // fim do método getScreen

    // retorna a referência ao banco de dados da instituição financeira
    public BankDatabase getBankDatabase()
    {
    } // fim do método getBankDatabase

    // método abstrato sobrescrito por subclasses
    public abstract void execute();
*/
} // fim da classe Transaction
